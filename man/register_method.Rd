% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register.R
\name{register_method}
\alias{register_method}
\title{Register a Custom Sampling Method}
\usage{
register_method(
  name,
  type = c("wor", "wr", "balanced"),
  sample_fn,
  joint_fn = NULL,
  fixed_size = TRUE,
  variance_family = NULL,
  supports_prn = FALSE,
  supports_aux = TRUE,
  supports_strata = FALSE,
  supports_spread = FALSE,
  probabilities = c("unknown", "exact", "approximate")
)
}
\arguments{
\item{name}{A unique method name (character string). Must not
collide with a built-in method name.}

\item{type}{\code{"wor"} (without replacement), \code{"wr"} (with
replacement), or \code{"balanced"} (balanced without replacement,
dispatched through \code{\link[=balanced_wor]{balanced_wor()}}).}

\item{sample_fn}{A function that draws a sample. See \strong{Contracts}
below.}

\item{joint_fn}{An optional function that computes joint inclusion
probabilities (WOR) or joint expected hits (WR). If \code{NULL},
\code{\link[=joint_inclusion_prob]{joint_inclusion_prob()}} / \code{\link[=joint_expected_hits]{joint_expected_hits()}} will error for
this method.}

\item{fixed_size}{Does this method always produce exactly \code{n} units?
Must be \code{TRUE} for \code{type = "wr"}.}

\item{variance_family}{Optional declaration of how design-based
variance should be estimated for this method, for downstream
packages that export designs for variance estimation. One of
\code{"srs"}, \code{"pps_brewer"}, \code{"poisson"}, \code{"wr"}, or
\code{"unsupported"}; see \strong{Variance families} below. \code{NULL} (the
default) means undeclared: consumers fall back on inferring a
treatment from \code{type} and \code{fixed_size}.}

\item{supports_prn}{Does this method support permanent random
numbers for sample coordination? Only \code{"wor"} and \code{"wr"} methods;
\code{\link[=balanced_wor]{balanced_wor()}} has no \code{prn} argument.}

\item{supports_aux}{Does this method use auxiliary balancing
variables? Only meaningful for \code{type = "balanced"}. Set it to
\code{FALSE} for spread-only (spatially balanced) methods such as the
local pivotal method, so that passing \code{aux} to \code{\link[=balanced_wor]{balanced_wor()}}
is an error instead of being silently ignored.}

\item{supports_strata}{Does this method support stratified balanced
sampling? Only meaningful for \code{type = "balanced"}. When \code{FALSE}
(the default), passing \code{strata} to \code{\link[=balanced_wor]{balanced_wor()}} with this
method is an error, and \code{sample_fn} does not need a \code{strata}
argument.}

\item{supports_spread}{Does this method support spatial spreading
(well-spread / spatially balanced sampling)? Only meaningful for
\code{type = "balanced"}. When \code{FALSE} (the default), passing \code{spread}
to \code{\link[=balanced_wor]{balanced_wor()}} with this method is an error, and \code{sample_fn}
does not need a \code{spread} argument.}

\item{probabilities}{Where the method sits in the first-order
probability taxonomy, for downstream packages that weight or
record per-unit selection probabilities:
\itemize{
\item \code{"exact"}: the true first-order inclusion probabilities
(types \code{"wor"} and \code{"balanced"}) or expected hits (type
\code{"wr"}) equal the \code{pik} vector passed to \code{sample_fn}, as for
Sampford or the cube method.
\item \code{"approximate"}: \code{pik} is the method's first-order target,
achieved up to a documented approximation, as for Pareto or
sequential Poisson order sampling. Design weights \code{1/pik}
remain standard practice.
\item \code{"unknown"} (the default): \code{pik} is an input weight or
preference only, so the true first-order probabilities are
not known. The toy sampler in the examples is such a method:
successive sampling with \code{prob = pik} does not yield inclusion
probabilities equal to \code{pik}, so its design weights \code{1/pik}
would be systematically biased, not merely noisy. (The same
draw \emph{with} replacement does honor expected hits, so a
multinomial-style \code{type = "wr"} method declares \code{"exact"}.)
}

The default is deliberately strict: if you have not established
which tier your method is in, its selection probabilities are
unknown, and downstream packages that weight estimation by
\code{1/pik} may refuse to draw with it rather than produce biased
weights. Sampling through sondage itself is unaffected: the
declaration describes the method, it never disables it. Like
\code{variance_family}, the declaration is an assertion by the method
author that sondage cannot verify; the package vignette shows
how to check a first-order contract by simulation.}
}
\value{
Invisible \code{NULL}, called for its side effect.
}
\description{
Register a user-defined sampling method so it can be used through
\code{\link[=unequal_prob_wor]{unequal_prob_wor()}}, \code{\link[=unequal_prob_wr]{unequal_prob_wr()}}, or \code{\link[=balanced_wor]{balanced_wor()}} and
their associated generics.
}
\section{Contracts}{


\strong{\code{sample_fn(pik, n = NULL, prn = NULL, ...)}} (types \code{"wor"} and \code{"wr"})
\describe{
\item{\code{pik}}{Inclusion probabilities (WOR) or expected hits (WR),
numeric vector of length N.}
\item{\code{n}}{Target sample size (integer). For WOR this equals
\code{round(sum(pik))}; for WR \code{round(sum(hits))}.}
\item{\code{prn}}{Permanent random numbers (numeric vector length N,
values in (0,1)), or \code{NULL}. Always \code{NULL} when the method is
registered with \code{supports_prn = FALSE}.}
\item{Returns}{Integer vector of selected unit indices (1-based).
For WOR: distinct indices of length \code{n} (fixed-size) or varying
length (random-size). For WR: indices with possible repeats,
of length \code{n}. The dispatcher validates the type, range, size,
and replacement rules before constructing the sample object.}
}

\strong{\code{sample_fn(pik, n = NULL, aux = NULL, ...)}} (type \code{"balanced"})
\describe{
\item{\code{pik}}{Inclusion probabilities, numeric vector of length N.}
\item{\code{n}}{Target sample size (integer when \code{fixed_size},
otherwise \code{sum(pik)}).}
\item{\code{aux}}{Auxiliary balancing matrix (N x p, double), or
\code{NULL}. Passed through as supplied to \code{\link[=balanced_wor]{balanced_wor()}} after
validation; the sample-size constraint is \strong{not}
prepended, so add it yourself if your algorithm needs it
(e.g. \code{cbind(pik, aux)}). Methods registered with
\code{supports_aux = FALSE} always receive \code{aux = NULL}.}
\item{\code{strata}}{Only when registered with
\code{supports_strata = TRUE} and the caller supplies \code{strata}:
an integer vector (length N) of dense stratum labels \code{1:H}.
Declare it as \code{strata = NULL} in your function signature.}
\item{\code{spread}}{Only when registered with
\code{supports_spread = TRUE} and the caller supplies \code{spread}:
a numeric matrix (N x d, double) of spatial coordinates (or
other spreading variables). Declare it as \code{spread = NULL} in
your function signature.}
\item{Returns}{Integer vector of distinct selected unit indices
(1-based). The dispatcher validates the returned indices.}
}

\strong{\code{joint_fn(pik, sample_idx = NULL, ...)}} (optional)
\describe{
\item{\code{pik}}{Same as above.}
\item{\code{sample_idx}}{When non-NULL, an integer vector of sampled
unit indices. Return only the submatrix for these units.}
\item{Returns}{Symmetric matrix of joint inclusion probabilities
(N x N when \code{sample_idx} is NULL, \code{length(sample_idx)} x
\code{length(sample_idx)} otherwise). The dispatcher validates that
the matrix has the required dimensions and contains finite,
symmetric numeric values.}
}
}

\section{Variance families}{


\code{variance_family} names the estimator treatment a variance consumer
(such as a survey-export package) should apply to samples drawn
with this method. Selection metadata alone cannot determine it: a
fixed-size WOR method may need Brewer's unequal-probability
approximation or an SRS-style variance, and for a random-size WOR
method no safe inference exists at all, because a Poisson-type
method (independent selections) and a correlated random-size
scheme need different estimators.

\describe{
\item{\code{"srs"}}{Equal-probability fixed-size WOR. SRS-style
variance with a finite population correction. Requires
\code{fixed_size = TRUE}.}
\item{\code{"pps_brewer"}}{Fixed-size unequal-probability WOR.
Brewer's approximation from the marginal inclusion
probabilities. Requires \code{fixed_size = TRUE}.}
\item{\code{"poisson"}}{Random-size WOR with \strong{independent}
selections (Poisson-type). Exact Poisson linearization.
Requires \code{type = "wor"} and \code{fixed_size = FALSE}.}
\item{\code{"wr"}}{With-replacement (or minimum-replacement)
selection. Hansen-Hurwitz variance, no finite population
correction. Requires \code{type = "wr"}.}
\item{\code{"unsupported"}}{No linearization treatment is valid;
consumers should refuse to linearize and point to replicate
methods instead. The correct declaration for correlated
random-size schemes. Always allowed.}
}

Balanced methods allow only \code{"pps_brewer"} or \code{"unsupported"}:
balancing constraints couple selections across units, so
\code{"poisson"} can never hold for them.

The declaration is an assertion by the method author, not
something sondage can verify; \code{"poisson"} in particular asserts
that units are selected independently, and a wrong declaration
produces silently wrong variance estimates for every user of the
method. The package vignette
(\code{vignette("custom-methods", package = "sondage")}) shows how to
check a declared family by simulation.
}

\examples{
# Register a toy random sampler. Successive sampling with
# prob = pik does not give first-order inclusion probabilities
# equal to pik, so its probabilities stay "unknown" (the default).
my_sampler <- function(pik, n = NULL, prn = NULL, ...) {
  sample.int(length(pik), size = n, prob = pik)
}
register_method(
  "toy",
  type = "wor",
  sample_fn = my_sampler
)
s <- unequal_prob_wor(c(0.3, 0.3, 0.4), method = "toy")
s$method

# Register a toy balanced sampler (ignores aux, keeps size fixed)
my_balanced <- function(pik, n = NULL, aux = NULL, ...) {
  sample.int(length(pik), size = n, prob = pik)
}
register_method("toy_bal", type = "balanced", sample_fn = my_balanced)
s <- balanced_wor(c(0.3, 0.3, 0.4), method = "toy_bal")
s$method

# Clean up
unregister_method("toy")
unregister_method("toy_bal")

}
\seealso{
\code{\link[=registered_methods]{registered_methods()}}, \code{\link[=method_spec]{method_spec()}},
\code{\link[=unequal_prob_wor]{unequal_prob_wor()}}, \code{\link[=unequal_prob_wr]{unequal_prob_wr()}}, \code{\link[=balanced_wor]{balanced_wor()}}
}
