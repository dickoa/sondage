% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/srs.R
\name{srs}
\alias{srs}
\title{Simple Random Sampling}
\usage{
srs(n, N, replace = FALSE)
}
\arguments{
\item{n}{Sample size (number of units to select).}

\item{N}{Population size.}

\item{replace}{Logical. Sample with replacement? Default is \code{FALSE}.}
}
\value{
An integer vector of selected indices (1 to N).
Length is \code{n}. With \code{replace = TRUE}, indices may repeat.
}
\description{
Draws a simple random sample of n units from a population of N units.
}
\details{
Without replacement (\code{replace = FALSE}):
\itemize{
\item Each possible sample of size n has equal probability
\item Inclusion probability: \eqn{\pi_k = n/N} for all units
\item n must not exceed N
}

With replacement (\code{replace = TRUE}):
\itemize{
\item Each draw is independent with probability 1/N per unit
\item Same unit can be selected multiple times
\item n can exceed N
}
}
\examples{
# Without replacement
set.seed(42)
idx <- srs(3, 10)
idx

# Select from data frame
df <- data.frame(id = 1:10, x = rnorm(10))
df[idx, ]

# With replacement (can have repeats)
set.seed(42)
idx <- srs(5, 10, replace = TRUE)
idx
df[idx, ]  # Some rows may appear twice

}
\seealso{
\code{\link[=systematic]{systematic()}} for systematic sampling, \code{\link[=bernoulli]{bernoulli()}} for Bernoulli sampling
}
