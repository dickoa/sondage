% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unequal_prob.R
\name{unequal_prob_wor}
\alias{unequal_prob_wor}
\title{Unequal Probability Sampling Without Replacement}
\usage{
unequal_prob_wor(
  pik,
  method = c("cps", "brewer", "systematic", "poisson", "sps", "pareto"),
  nrep = 1L,
  prn = NULL,
  ...
)
}
\arguments{
\item{pik}{A numeric vector of inclusion probabilities.
For fixed-size methods, \code{sum(pik)} must be close to an integer.}

\item{method}{The sampling method:
\describe{
\item{\code{"cps"}}{Conditional Poisson Sampling (maximum entropy;
Chen et al., 1994). Fixed size, exact joint probabilities
with all \eqn{\pi_{ij} > 0}. O(N^2) to build the ESF table,
O(Nn) per draw thereafter.}
\item{\code{"brewer"}}{Brewer's (1975) draw-by-draw method. Fixed
size, approximate joint probabilities (high-entropy
approximation; see \code{\link[=joint_inclusion_prob]{joint_inclusion_prob()}}). O(Nn).}
\item{\code{"systematic"}}{Systematic PPS. Fixed size, exact joint
probabilities but \strong{some may be zero} (pairs that never
co-occur), making the SYG estimator inapplicable; see
\code{\link[=sampling_cov]{sampling_cov()}}. O(N).}
\item{\code{"poisson"}}{Poisson sampling. \strong{Random} sample
size (expected \eqn{n = \sum \pi_k}). Units selected
independently, so \eqn{\pi_{ij} = \pi_i \pi_j}. Supports
PRN. O(N).}
\item{\code{"sps"}}{Sequential Poisson Sampling (Ohlsson, 1998).
Order sampling with key \eqn{\xi_k = u_k / \pi_k}; the
\eqn{n} smallest are selected. Fixed size, high-entropy.
Supports PRN. Approximate joint probabilities. The true
first-order inclusion probabilities are approximately equal
to the supplied \code{pik}; see \code{\link[=inclusion_prob]{inclusion_prob()}}.
O(N log N).}
\item{\code{"pareto"}}{Pareto sampling (Rosen, 1997). Order
sampling with odds-ratio key
\eqn{\xi_k = [u_k/(1-u_k)] / [\pi_k/(1-\pi_k)]}. Same
properties as \code{"sps"}. O(N log N).}
}}

\item{nrep}{Number of replicate samples (default 1). When \code{nrep > 1},
\verb{$sample} holds a matrix (fixed-size) or list (random-size) of all
replicates. The design object and all generics remain usable.}

\item{prn}{Optional vector of permanent random numbers (length N,
values in the open interval (0, 1)) for sample coordination.
Supported by methods \code{"sps"}, \code{"pareto"}, and \code{"poisson"}.
When \code{NULL}, random numbers are generated internally.
Cannot be used with \code{nrep > 1} (identical PRN would produce
identical replicates). Use a loop with different PRN vectors
for coordinated repeated sampling.}

\item{...}{Additional arguments passed to methods (e.g., \code{eps} for
boundary tolerance; see Details).}
}
\value{
An object of class \code{c("unequal_prob", "wor", "sondage_sample")}.
When \code{nrep = 1}, \verb{$sample} is an integer vector of selected unit indices.
When \code{nrep > 1}, \verb{$sample} is a matrix (n x nrep) for fixed-size methods,
or a list of integer vectors of varying lengths for random-size methods (\code{"poisson"}).
\verb{$n} is an integer for fixed-size methods (realized size) and a
double for \code{"poisson"} (expected size, \code{sum(pik)}); see
\link{sondage_sample}.
}
\description{
Draws a sample with unequal inclusion probabilities, without replacement.
}
\details{
\strong{Near-certainty inclusion probabilities (CPS).} The CPS Newton
calibration converges geometrically for well-spread \code{pik}, but
asymptotes at a non-zero defect when some \code{pik} are within a few
decimal digits of 0 or 1 (e.g. 0.9999). When this happens the
function emits a "CPS calibration did not reach tolerance" warning
reporting the achieved \code{max_diff}. The realized first-order
inclusion probabilities differ from the target by up to \code{max_diff} --
typically 1e-5 or smaller for inputs in the 0.999-range, well within
Monte Carlo error for most estimators. If the warning is unwanted,
clip \code{pik} away from 0/1 before calling.
}
\examples{
pik <- c(0.2, 0.4, 0.6, 0.8)

# Conditional Poisson Sampling
set.seed(123)
s <- unequal_prob_wor(pik, method = "cps")
s$sample

# Brewer's method
s <- unequal_prob_wor(pik, method = "brewer")
s$sample

# Sequential Poisson Sampling with PRN coordination
prn <- runif(4)
s <- unequal_prob_wor(pik, method = "sps", prn = prn)
s$sample

# Pareto sampling
s <- unequal_prob_wor(pik, method = "pareto", prn = prn)
s$sample

\donttest{
# Batch mode for simulations
sim <- unequal_prob_wor(pik, method = "cps", nrep = 1000)
dim(sim$sample)  # 2 x 1000
}

}
\references{
Chen, X. H., Dempster, A. P., & Liu, J. S. (1994). Weighted finite
population sampling to maximize entropy. \emph{Biometrika}, 81(3),
457-469.

Brewer, K.R.W. (1975). A simple procedure for sampling pi-ps wor.
\emph{Australian Journal of Statistics}, 17(3), 166-172.

Ohlsson, E. (1998). Sequential Poisson sampling. \emph{Journal of
Official Statistics}, 14(2), 149-162.

Rosen, B. (1997). On sampling with probability proportional to size.
\emph{Journal of Statistical Planning and Inference}, 62(2), 159-191.

Tille, Y. (2006). \emph{Sampling Algorithms}. Springer.
}
\seealso{
\code{\link[=unequal_prob_wr]{unequal_prob_wr()}} for with-replacement designs,
\code{\link[=equal_prob_wor]{equal_prob_wor()}} for equal probability designs,
\code{\link[=inclusion_prob]{inclusion_prob()}} to compute inclusion probabilities from size measures.
}
